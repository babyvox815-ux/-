# -
테스트

## Local agent setup notes

Configured local Lazyweb prerequisites for Codex runtime:
- token file path: `~/.lazyweb/lazyweb_mcp_token` (local-only, not committed)
- Codex config file: `${CODEX_HOME:-$HOME/.codex}/config.toml`
- attempted marketplace install for `https://github.com/aboul3ata/lazyweb-skill`

If `codex` CLI is not available on PATH, install/enable the CLI first and rerun plugin marketplace commands.
